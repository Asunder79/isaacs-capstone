//Contains functions that perform **CRUD** (Create, Read, Update, Delete) operations, 
// a more modular approach which separates the database logic from the routing.
// increases navigation and dev complexity, maybe merge with other files? idk yet. 