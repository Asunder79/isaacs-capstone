//Cycles through a predefined array of image URLs (or fetches them from an API), 
// and updates the `<body>` background image property on a timed interval. 
//This idea maybe scrapped, time constraint

